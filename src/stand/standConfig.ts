import { createConfig } from '@consta/stand';

export const { createStand, createPage } = createConfig({
  title: 'Конструкор темы',
  id: 'theme-constuctor',
  groups: [],
  group: 'Конструкор темы',
  repositoryUrl: 'https://github.com/consta-design-system/portal',
});
